info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{urlBase}}/api/index.php"
  body:
    type: json
    data: |-
      {
        "login": "testuser",
        "password": "TEST_HASH"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
