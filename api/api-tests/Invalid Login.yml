info:
  name: Invalid Login
  type: http
  seq: 3

http:
  method: POST
  url: "{{urlBase}}/api/index.php"
  body:
    type: json
    data: |-
      {
        "login": "RickL",
        "password": "WRONG_PASSWORD"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
