info:
  name: Status Ping
  type: http
  seq: 1

http:
  method: GET
  url: "{{urlBase}}/api/index.php?ping=1"
  params:
    - name: ping
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
