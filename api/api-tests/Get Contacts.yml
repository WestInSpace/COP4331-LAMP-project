info:
  name: Get Contacts
  type: http
  seq: 6

http:
  method: GET
  url: "{{urlBase}}/api/index.php?id=5"
  headers:
    - name: Authorization
      value: Bearer 2
  params:
    - name: id
      value: "5"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
