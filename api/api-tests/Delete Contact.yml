info:
  name: Delete Contact
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{urlBase}}/api/index.php?id=5"
  headers:
    - name: Authorization
      value: Bearer 2
  params:
    - name: id
      value: "5"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
